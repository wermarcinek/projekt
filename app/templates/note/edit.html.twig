{% include 'default/_form.html.twig' with {
    page_title: 'title.note_edit'|trans({'%id%': note.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'note_index',
} %}